[project]
name = "devflow-app"
version = "0.1.0"
description = "DevFlow Agent — developer workflow automation"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
